# ─────────────────────────────────────────────────────────────────────────────
# TitlePro CURE — Python Dependencies
# Install: pip install -r requirements.txt
#
# UAT / Ubuntu server also needs the Tesseract system package:
#   sudo apt-get install -y tesseract-ocr
# Windows: install Tesseract from https://github.com/UB-Mannheim/tesseract/wiki
# ─────────────────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────────────────
Flask>=2.3.0
flask-cors>=4.0.0

# ── Browser automation (Selenium — used for counties without HTTP adapter) ────
selenium>=4.15.0
webdriver-manager>=4.0.0

# ── PDF processing ────────────────────────────────────────────────────────────
PyMuPDF>=1.23.0           # imported as 'fitz' — primary PDF text extractor
reportlab>=4.0.0          # legacy PDF generation (title report renderer)
pdfminer.six>=20221105    # fallback PDF text extraction

# ── OCR (scanned / image-only PDFs) ──────────────────────────────────────────
# IMPORTANT: also requires the Tesseract binary on the OS:
#   Ubuntu: sudo apt-get install -y tesseract-ocr
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
Pillow>=10.0.0            # image processing for OCR + CAPTCHA
pytesseract>=0.3.13       # Python wrapper for Tesseract OCR

# ── PDF rendering (OnE Report / Title Notes → PDF) ───────────────────────────
weasyprint>=60.0          # HTML/CSS → PDF renderer (used by renderers.py)
xhtml2pdf>=0.2.11         # fallback HTML → PDF (legacy renderer)

# ── HTTP clients ──────────────────────────────────────────────────────────────
requests>=2.31.0          # standard HTTP client
curl_cffi>=0.7.0          # TLS-fingerprint impersonation — REQUIRED for Broward/Cloudflare counties

# ── HTML / web scraping ───────────────────────────────────────────────────────
beautifulsoup4>=4.12.0    # HTML parsing for property-appraiser adapters
lxml>=4.9.0               # fast XML/HTML parser (used by bs4 + xhtml2pdf)

# ── AI / LLM providers ───────────────────────────────────────────────────────
anthropic>=0.25.0         # Claude API — primary AI engine (AI_ENGINE=claude in secrets.json)
google-generativeai>=0.5.0  # Gemini API — optional (AI_ENGINE=google)
google-auth>=2.28.0       # Google service-account JSON auth for Gemini
groq>=0.5.0               # Groq LLaMA — optional (AI_ENGINE=groq)
openai>=1.12.0            # OpenAI GPT — optional fallback

# ── Environment / config ──────────────────────────────────────────────────────
python-dotenv>=1.0.0      # loads .env files (used in some adapters)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-mock>=3.12.0
